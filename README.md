# IBM-HR-Attrition-Prediction
Built a supervised classification model to predict employee attrition (Yes/No) using the IBM HR dataset (1470 rows, 35 features). Performed data quality checks (no missing values, no duplicates), basic EDA, and feature preparation. Trained and tuned a Random Forest model using GridSearchCV.
